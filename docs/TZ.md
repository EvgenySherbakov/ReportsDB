# ТЗ: локальная база данных отчётности SSRS + аналитика

**Русский** · [English](TZ.en.md)

> Документ является **источником истины** для проекта. Любой AI-агент, продолжающий
> работу, обязан прочитать его целиком перед первым изменением кода и обновить
> раздел [15. Журнал решений](#15-журнал-решений) после внесения изменений.

---

## 1. Контекст и цель

В организации эксплуатируется набор отчётов SSRS (SQL Server Reporting Services).
Информация о них живёт в Excel-файле и не поддаётся анализу: нельзя ответить на
вопросы «какие отчёты можно вывести из эксплуатации», «какие таблицы критичны»,
«сколько данных обслуживает каждый отчёт».

**Цель:** превратить Excel-выгрузку в локальную реляционную БД с готовой
аналитикой, которую можно передать коллеге одним файлом/архивом и запустить без
администраторских прав и без сервера.

**Не является целью:** замена SSRS, онлайн-мониторинг, многопользовательская
работа, запись данных пользователями.

---

## 2. Ключевые решения (зафиксированы с заказчиком)

| Вопрос | Решение | Обоснование |
| --- | --- | --- |
| СУБД | **DuckDB** (файл `data/reports.duckdb`) | Встраиваемая, один файл, нулевая установка, колоночная — идеальна для аналитических агрегатов. SQL-диалект близок к PostgreSQL. |
| Визуализация, вариант A | **Streamlit + Plotly** | Полноценное интерактивное приложение, произвольный SQL, фильтры. Для коллег с Docker или Python. |
| Визуализация, вариант B | **Самодостаточный HTML-файл** | Для коллег, у которых нет ничего кроме браузера. Передаётся по почте, открывается двойным кликом. |
| Доставка коллегам | Docker-образ **или** один HTML-файл | Заказчик подтвердил: на машинах доступны Docker и браузер. Python не гарантирован. |
| Режим загрузки данных | **Разовая полная пересборка** (`full refresh`) | Историчность не требуется. ETL идемпотентен: повторный запуск пересоздаёт БД с нуля. |
| Приоритет аналитики | **Объём данных и стоимость** | Главный сценарий — сколько данных обслуживает каждый отчёт и что это стоит. |

### 2.1. Осознанное отклонение по варианту B

Изначально обсуждался DuckDB-WASM внутри HTML. **Реализовано иначе:** данные
встраиваются в HTML как JSON, агрегация выполняется на чистом JavaScript.

Причина: DuckDB-WASM при открытии страницы по протоколу `file://` требует
загрузки `.wasm` и worker-скрипта, что блокируется политикой CORS браузера;
обход через инлайн base64 раздувает файл до десятков мегабайт и остаётся
хрупким. Объём данных (сотни отчётов, тысячи таблиц) на порядки меньше порога,
где нужен движок БД. Требование заказчика — «открывается двойным кликом, ничего
ставить не надо» — выполняется полностью и надёжнее.

Если в будущем HTML будет раздаваться по HTTP, переход на DuckDB-WASM возможен
без изменения модели данных.

---

## 3. Исходные данные

### 3.1. Файл, доступный сейчас

Excel-файл (`.xls`/`.xlsx`) со строкой на отчёт. **Фактические заголовки
подтверждены заказчиком:**

| Заголовок в файле | Поле модели | Смысл |
| --- | --- | --- |
| `№` | `report_no` | Номер строки в исходной таблице |
| `ТС` | `network` | Торговая сеть |
| `Завод` | `plant` | Завод внутри сети |
| `Каталог 1-го уровня` | `folder_l1` | Путь пользователя к отчёту, уровень 1 |
| `Каталог 2-го уровня` | `folder_l2` | Уровень 2 |
| `Каталог 3-го уровня` | `folder_l3` | Уровень 3 |
| `Наименование отчета` | `report_name` | Имя отчёта (обязательное поле) |
| `Используется view` | `uses_view` | Обращается ли отчёт к данным через view |
| `Таблицы источники данных` | `source_tables` | Список таблиц через `;` |
| `View` | `source_views` | Список view через `;` |
| `Mat.view` | `source_matviews` | Список материализованных view |
| `Временные таблицы` | `source_temp_tables` | Временные и generated-объекты |
| `Функции/процедуры` | `source_routines` | Список функций и процедур |

Заказчик показал реальную форму основного файла: в нём физически бывают
колонки `Ср. дл. (сек)` и `Кол-во обращений` (старый формат выгрузки), но они
**не читаются** — `reports.columns` в `mapping.yml` этих полей не знает.
Единственный источник статистики — отдельный файл, см. ниже.

Правила разбора:

- Список таблиц лежит **в одной ячейке**, элементы в формате `схема.таблица`,
  разделитель `;`.
- `catalog_path` **собирается** из трёх колонок уровней: пустые уровни
  пропускаются, слэш внутри значения заменяется на дефис. Одна колонка
  «Каталог» поддерживается как запасной вариант для файлов старой структуры.
- Ключ уникальности отчёта — `(network, plant, catalog_path, report_name)`:
  один и тот же отчёт существует для разных сетей и заводов, схлопывать такие
  строки нельзя.
- `report_no` хранится **текстом** (встречаются значения вида `1.2`).
- `uses_view` разбирается терпимо: `да/нет`, `yes/no`, `1/0`, `+/-`.
  Непонятное значение даёт `NULL`, а не `FALSE` — «неизвестно» и «нет» ведут к
  разным выводам.
- `Наименование отчета` написано без «ё»; сопоставление приводит `ё` к `е`,
  поэтому оба написания работают.

**Тип объекта определяется колонкой, из которой объект пришёл** — это надёжнее
догадок по имени: таблица `V_SALES` останется таблицей, если лежит в колонке
таблиц. В `dim_table.kind_source` записывается, откуда тип известен:
«колонка», «маска» или «по умолчанию».

Маски имён (`reports.object_patterns` в `mapping.yml`) — **последняя догадка**,
и работают они только для колонки «Таблицы источники данных». По умолчанию
включена ровно одна: `VIEW: ["V_*", "VW_*"]`. Список закреплён тестом, потому
что неверно угаданный тип молча выкинул бы таблицу из таблицы №2 и из расчёта
объёма — новая маска добавляется осознанно, а не мимоходом.

**Маска не применяется к объектам, найденным в файле размеров.** Наличие
сегментов означает, что объект физически хранится, — это таблица, как бы она ни
называлась. Для этого перед разбором отчётов по файлу размеров строится не
только индекс схем (см. 7.2.1), но и множество всех его имён.

**Статистика использования — только отдельный файл.** Колонки `Кол-во
обращений` и `Ср. дл. (сек)` в основном файле не читаются ни при каких
условиях; `fact_report_usage` заполняет исключительно секция `report_usage`.
Так было не всегда: до этого решения статистика бралась прямо из основного
файла, а отдельный файл был опциональным перекрытием — заказчик прямо попросил
развести источники, потому что основной файл присылают реже и статистика в нём
устаревает. Сопоставление строки с отчётом идёт по трём уровням, от точного к
грубому: `(сеть, завод, каталог, имя)` → `(сеть, завод, имя)` → одно имя, если
оно единственное в базе. Второй уровень существует специально для файлов без
колонки «Каталог» (обычная форма выгрузки статистики): без него одинаковое имя
отчёта на разных заводах делало сопоставление неоднозначным, и строка просто
терялась.

**Текст SQL-запроса — тоже отдельный, необязательный файл** (`report_sql`).
Структура повторяет файл отчётов, заказчик выгружает его так:

| Заголовок в файле | Поле модели |
| --- | --- |
| `№`, `ТС`, `Завод` | для сопоставления, `report_no` из него не берётся |
| `Каталог 1/2/3-го уровня` | путь собирается так же, как в файле отчётов |
| `Наименование отчета` | `report_name` |
| `Запрос к базе данных` | `sql_text` |

Сопоставление по трём уровням, от точного к грубому: `(сеть, завод, каталог,
имя)` → `(сеть, завод, имя)` → одно имя. **Третий уровень отличается от
статистики использования:** если колонок ТС и Завода в файле нет вовсе, текст
применяется ко **всем** отчётам с этим именем — запрос описывает определение
отчёта, а не площадку, и общий текст для тёзки на соседнем заводе не ошибка, а
точный смысл данных. Если ТС с Заводом в файле есть, но строка ими не
сопоставилась, имя годится только когда оно единственное в базе: иначе запрос
ушёл бы на чужой завод, а у того свой.

Хранится в `dim_report.sql_text` (`SCHEMA_VERSION` поднята до 13). Показывается
на странице «Инструменты → Запросы к БД» (см. 5.1) и на отдельной вкладке
карточки отчёта.

### 3.1.1. Данные не покидают машину

Заказчик работает с рабочими данными, **исходные файлы и собранная БД в
репозиторий не попадают**. Обеспечено `.gitignore`: `data/raw/*`,
`data/*.duckdb`, `dist/`. В git лежит только код, SQL и конфиг с именами
колонок. Проверяется тестом `test_raw_data_is_git_ignored`.

Отсюда следствия, которые обязан соблюдать любой агент:

- никогда не коммитить содержимое `data/` и `dist/`;
- не вставлять реальные имена отчётов, таблиц и схем в код, тесты, README,
  сообщения коммитов и описания PR;
- для примеров использовать только синтетику из `python -m reportsdb sample`;
- Docker-образ содержит `data/` — он предназначен для передачи внутри команды
  и не должен публиковаться в открытых реестрах.

### 3.2. Файл размеров — выгрузка сегментов БД

Второй файл: строка на **сегмент**, а не на таблицу.

| Заголовок | Поле модели | Смысл |
| --- | --- | --- |
| `№` | — | Порядковый номер, не используется |
| `ТС` | `network` | Торговая сеть. Необязательна |
| `Завод` | `plant` | Завод. Необязателен |
| `OWNER` | `schema_name` | Схема |
| `SEGMENT_NAME` | `table_name` | Имя сегмента |
| `SEGMENT_TYPE` | `segment_type` | `TABLE`, `TABLE PARTITION`, `INDEX`, … |
| `SIZE_MB` | `total_mb` | Размер сегмента, МБ |
| `PERCENT_OF_TOTAL` | `percent_of_total` | Доля в общем объёме БД, % |
| `Глубина хранения` | `retention_days` | Сколько дней данных хранит таблица |
| `PERCENT_OF_SCHEMA` | — | Доля в схеме, не используется |

Четыре правила, без которых цифры будут неверными:

1. **Сегменты одной таблицы суммируются.** У секционированной таблицы
   несколько строк выгрузки; `SIZE_MB` и `PERCENT_OF_TOTAL` складываются,
   число сложенных строк пишется в `segment_count`.
2. **Учитываются только сегменты типа `TABLE*`.** Список задан в
   `mapping.yml` (`table_sizes.segment_types`). Индексные и LOB-сегменты
   носят собственные имена, привязать их к таблице по этой выгрузке нельзя —
   поэтому размер индексов в объём таблицы **не входит**. Число пропущенных
   сегментов выводится в сводке загрузки.
3. **Размер ведётся по заводам.** Одна и та же таблица на разных заводах
   занимает разное место, поэтому сегменты складываются в разрезе
   `(таблица, ТС, завод)`. Если колонок `ТС` и `Завод` в файле нет, строки
   попадают в «(не указана)» / «(не указан)» и применяются ко всем отчётам.
4. **Файл размеров — единственный источник списка таблиц БД.** Загружаются
   **все** его строки, а не только таблицы из отчётов: таблица, которой нет ни
   в одном отчёте, заводится в `dim_table` с `kind_source = 'файл размеров'` и
   попадает в каталог `v_tables_catalog`. Обратное неверно: объект, который
   упомянут в отчёте, но отсутствует в файле размеров, в каталог **не
   добавляется** — мост «отчёт ↔ таблица» лишь ссылается на этот список.
   Число таблиц, встретившихся только в файле размеров, выводится в сводке
   загрузки; сколько ссылок отчётов повисло без размера, видно в
   `size_coverage_pct` и подписью под таблицей №1.

### 3.2.1. Что появится позже

Число строк по таблицам (`row_count`) и границы периода статистики. Поля в
модели есть, при отсутствии данных остаются пустыми — витрины к этому готовы.

### 3.3. Правило: никаких предположений о колонках

Имена колонок в Excel заранее неизвестны и могут быть на русском. Поэтому:

- Соответствие «колонка файла → поле модели» задаётся **только** в
  `config/mapping.yml`.
- Перед первой загрузкой агент обязан выполнить `reportsdb profile <файл>`,
  посмотреть реальные заголовки и заполнить маппинг.
- **Запрещено** хардкодить имена колонок в коде ETL.
- Сопоставление ведётся без учёта регистра и краевых пробелов; поддерживается
  список синонимов на поле.

---

## 4. Модель данных

Схема «звезда» с мостом для связи многие-ко-многим.

```
        dim_report ──< bridge_report_table >── dim_table
             │                                     │
             │                                     │
      fact_report_usage                     fact_table_size
```

### 4.1. `dim_report` — отчёты

| Поле | Тип | Описание |
| --- | --- | --- |
| `report_id` | `INTEGER PK` | Суррогатный ключ |
| `report_no` | `VARCHAR` | Номер из колонки «№» исходного файла; текстом |
| `report_name` | `VARCHAR NOT NULL` | Имя отчёта |
| `network` | `VARCHAR` | Торговая сеть |
| `plant` | `VARCHAR` | Завод |
| `uses_view` | `BOOLEAN` | Отчёт обращается к данным через view |
| `catalog_path` | `VARCHAR` | Путь, собранный из трёх колонок уровней |
| `folder_l1`…`folder_l3` | `VARCHAR` | Первые три уровня папок — для группировок |
| `folder_depth` | `INTEGER` | Глубина вложенности |
| `description` | `VARCHAR` | Опционально |
| `owner` | `VARCHAR` | Опционально |
| `sql_text` | `VARCHAR` | Текст SQL-запроса — из отдельного файла `report_sql`, по имени отчёта |
| `source_row` | `INTEGER` | Номер строки в исходном файле — для трассировки |

`UNIQUE (network, plant, catalog_path, report_name)`.

### 4.2. `dim_table` — таблицы-источники

| Поле | Тип | Описание |
| --- | --- | --- |
| `table_id` | `INTEGER PK` | Суррогатный ключ |
| `schema_name` | `VARCHAR NOT NULL` | Схема; если не определена — `(unknown)` |
| `table_name` | `VARCHAR NOT NULL` | Имя таблицы |
| `full_name` | `VARCHAR NOT NULL UNIQUE` | `schema_name.table_name`, нижний регистр |
| `object_kind` | `VARCHAR` | `TABLE` / `VIEW` / `MATERIALIZED VIEW` / `TEMP` / `ROUTINE` |
| `kind_source` | `VARCHAR` | Откуда известен тип: колонка / маска / по умолчанию |
| `schema_source` | `VARCHAR` | Откуда известна схема: `ссылка` (была в тексте) / `файл размеров` (восстановлена по единственному совпадению имени) / `не определена` |
| `is_parsed_ok` | `BOOLEAN` | Производная от `schema_source`: `TRUE`, если схема известна хоть как-то |

Нормализация: обрезка пробелов, снятие квадратных скобок `[dbo].[Orders]` →
`dbo.Orders`, приведение к нижнему регистру для ключа сопоставления.
Отображаемое имя сохраняется как есть.

### 4.3. `bridge_report_table` — мост

| Поле | Тип |
| --- | --- |
| `report_id` | `INTEGER` |
| `table_id` | `INTEGER` |

`PRIMARY KEY (report_id, table_id)` — дубликаты внутри одной ячейки схлопываются.

### 4.4. `fact_table_size` — размеры

Ключ — тройка `(table_id, network, plant)`: размер одной таблицы на разных
заводах разный, поэтому строк на таблицу столько, на скольких заводах она
встретилась в файле размеров.

| Поле | Тип | Описание |
| --- | --- | --- |
| `table_id` | `INTEGER PK` | |
| `network` | `VARCHAR PK` | Торговая сеть; «(не указана)», если её нет в файле |
| `plant` | `VARCHAR PK` | Завод; «(не указан)», если его нет в файле |
| `row_count` | `BIGINT` | Число строк |
| `data_mb` | `DOUBLE` | Данные, МБ |
| `index_mb` | `DOUBLE` | Индексы, МБ |
| `total_mb` | `DOUBLE` | Итого по всем сегментам таблицы |
| `percent_of_total` | `DOUBLE` | Доля таблицы в общем объёме БД, % |
| `segment_count` | `INTEGER` | Сколько строк выгрузки сложилось в эту таблицу |
| `retention_days` | `INTEGER` | Глубина хранения данных, дней |
| `measured_at` | `DATE` | Дата замера |

### 4.5. `fact_report_usage` — использование (заполняется позже)

| Поле | Тип | Описание |
| --- | --- | --- |
| `report_id` | `INTEGER PK` | |
| `exec_count` | `BIGINT` | Запусков за период |
| `distinct_users` | `INTEGER` | Уникальных пользователей |
| `avg_duration_sec` | `DOUBLE` | Средняя длительность выборки, **секунды** |
| `last_executed_at` | `DATE` | Последний запуск |
| `period_start`, `period_end` | `DATE` | Границы периода |

### 4.6. `etl_run` и `etl_reject` — служебные

`etl_run` фиксирует время загрузки, имя исходного файла, его SHA-256, число
строк и версию кода. `etl_reject` хранит строки, которые не удалось разобрать,
с причиной — молчаливая потеря данных недопустима.

---

## 5. Витрины (SQL-представления)

Создаются в `sql/02_views.sql`. Все обязаны корректно работать при пустых
`fact_table_size` / `fact_report_usage`.

### 5.0. `v_report_table_size` — размер таблицы для конкретного отчёта

Служебная витрина, на которую опираются все остальные. Раз размер хранится по
заводам, наивное `JOIN fact_table_size USING (table_id)` размножило бы строки
отчёта в столько раз, на скольких заводах живёт его таблица, и объём вырос бы
кратно. Витрина берёт для каждой пары «отчёт + таблица» **ровно одну** строку
размера: сначала замер завода самого отчёта, при его отсутствии — общий замер
«(не указана)» / «(не указан)».

Колонки: `report_id`, `table_id`, `total_mb`, `row_count`, `retention_days`,
`percent_of_total`, `segment_count`, `has_size` (размер найден),
`size_is_plant_specific` (найден именно для завода отчёта, а не общий).

**Правило для агента:** соединять отчёт с размером только через эту витрину.
Прямой join к `fact_table_size` в новых запросах — ошибка.

### 5.1. `v_report_footprint` — объём данных на отчёт ⭐ приоритет

Для каждого отчёта:

- `table_count` — число таблиц-источников;
- `gross_mb` — сумма `total_mb` всех его таблиц;
- `exclusive_mb` — сумма `total_mb` **только тех таблиц, которые не использует ни один другой отчёт**;
- `shared_mb` = `gross_mb − exclusive_mb`;
- `gross_rows`, `exclusive_rows` — то же по числу строк;
- `size_coverage_pct` — доля таблиц отчёта, для которых известен размер;
- `exclusive_pct_of_db` — какую долю всей БД занимают эксклюзивные таблицы
  отчёта; сумма по отчётам корректна, как и у `exclusive_mb`.

> **Критично.** `gross_mb` при суммировании по всем отчётам многократно
> учитывает общие таблицы и **не является** объёмом хранилища. Реальная выгода
> от вывода отчёта из эксплуатации — это `exclusive_mb`. Оба показателя
> обязаны присутствовать, а в UI рядом с `gross_mb` должно стоять пояснение.
> `size_coverage_pct` защищает от вывода по неполным данным.

### 5.2. `v_table_criticality` — критичность таблиц

`full_name`, `report_count` (сколько отчётов зависит), `total_mb`, `row_count`,
`is_orphan` (`report_count = 0`), список отчётов.

### 5.3. `v_report_cost_value` — стоимость против ценности

Соединяет footprint и usage. Стоимость меряется **двумя независимыми
величинами**: объёмом данных (`exclusive_mb`) и временем
(`total_duration_sec` = `exec_count` × `avg_duration_sec`). Отчёт может быть
дешёвым по данным и дорогим по времени — это разные поводы для действий.

Поля: `exclusive_mb`, `exec_count`, `avg_duration_sec`, `total_duration_sec`,
`mb_per_execution`, и `quadrant` ∈ {`Дорогой и невостребованный`,
`Дорогой и востребованный`, `Дешёвый и невостребованный`,
`Дешёвый и востребованный`, `Нет данных об использовании`}.
Границы квадрантов — медианы по непустым значениям.

### 5.4. `v_decommission_candidates` — кандидаты на вывод

Отчёты, отсортированные по убыванию `exclusive_mb`, среди тех, где
`exec_count = 0` или `NULL`. Колонка `confidence` понижается, если:

- данных об использовании нет;
- **отчёт обращается к данным через view** — за view стоят таблицы, которых
  нет в списке источников, поэтому объём такого отчёта посчитан не полностью;
- `size_coverage_pct < 100` — размеры известны не по всем таблицам.

### 5.5. `v_network_overview` — сравнение РЦ

Пары «сеть × завод», сведённые из двух независимых сторон: отчёты приходят из
файла отчётов, размеры — из файла размеров. Соединение `FULL JOIN`: завод
бывает в одном файле и отсутствует в другом, и такой РЦ обязан остаться в
сравнении со своими цифрами, а не исчезнуть.

Колонки: число отчётов и из них через view; объём базы завода и число таблиц в
ней; сколько таблиц и мегабайт под отчётами и сколько без них; доля объёма под
отчётами; число схем; медиана глубины хранения; освобождаемый объём; суммарное
число запусков и суммарное время; «МБ на отчёт».

### 5.5.1. `v_report_duration` — время выполнения

Отчёты, отсортированные по суммарному времени. Поля: `avg_duration_sec`,
`total_duration_sec`, `duration_band` (меньше секунды / от 1 с / от 10 с /
минута и дольше). Это **вторая метрика стоимости**, независимая от объёма.

### 5.5.2. `v_catalog_overview` и `v_schema_overview`

`v_catalog_overview` группирует по **всем трём уровням** каталога и несёт
`reports_with_view`, `exclusive_pct_of_db`, `exec_count`, `total_duration_sec`.
`v_schema_overview` добавляет `percent_of_db`.

Агрегаты по папкам каталога (`folder_l1`) и по схемам БД: число отчётов, число
таблиц, суммарный объём.

### 5.6. Пять основных представлений в разрезе РЦ ⭐

Заказчик определил их как основные. **РЦ — это пара «сеть + завод»:** одно имя
завода встречается в разных торговых сетях и означает разные площадки, поэтому
все пять витрин несут обе колонки, а уникальность считается по паре.

| № | Витрина | Что сопоставляет | Связь |
| --- | --- | --- | --- |
| 1 | `v_tables_catalog` | Таблица + завод и её размер | один к одному |
| 2 | `v_rc_report_tables` | Отчёт и его таблицы | один ко многим |
| 3 | `v_rc_report_routines` | Отчёт и его функции/процедуры | один ко многим |
| 4 | `v_rc_report_usage` | Отчёт и обращения пользователей | один к одному |
| 5 | `v_rc_report_retention` | Отчёт и глубина хранения | один к одному |

Плюс `v_rc_summary` — шапка страницы: сколько в РЦ отчётов, таблиц, view,
материализованных view, временных объектов и процедур.

Правила, без которых цифры будут неверными:

- **№1 — это ровно файл размеров, ни больше ни меньше.** Сначала существует
  каталог таблиц, и только потом — отчёты, которые часть из них используют.
  В витрину попадает **каждая** строка файла размеров, включая таблицы, к
  которым не тянется ни один отчёт, и **не попадает ничего сверх неё**:
  объекты из отчётов без замера в список не подмешиваются, мост
  «отчёт ↔ таблица» только ссылается на этот список. Единица строки — пара
  «таблица + завод», потому что на каждом заводе размер свой. Колонка
  `report_count` в №1 **справочная**: она отвечает на вопрос «используется ли
  таблица», но не отбирает строки. Показатели, считающие таблицы (например,
  «не используются отчётами»), берут `COUNT(DISTINCT full_name)`: счёт по
  строкам завысил бы их во столько раз, на скольких заводах живёт таблица.
- **№2 включает только `TABLE`.** View, mat.view, временные и
  generated-объекты отфильтрованы: они не отражают физического хранения.
- **Суммировать объём по всем РЦ сразу нельзя** — одна таблица обслуживает
  отчёты нескольких РЦ. В интерфейсе показатели считают каждый объект один
  раз, а расхождение между числом строк и числом объектов выводится явно.
- **№4 меряет обращения, а не уникальных пользователей.** Заказчик подтвердил,
  что мерой активности служит «Кол-во обращений». Колонка `distinct_users`
  остаётся в модели и заполнится, если появится отдельный источник.
- **Глубина отчёта — максимум по его таблицам.** Отчёт показывает столько
  дней, сколько хранит самая «долгая» его таблица. Границы: до 30 / 31–45 /
  более 45 дней, плюс «Не задана».

### 5.7. `v_report_overlap` — пересечение отчётов

Пары отчётов с коэффициентом Жаккара по множествам **настоящих таблиц**
(`object_kind = 'TABLE'`) ≥ 0.8 — кандидаты на объединение. Ограничение:
только пары, где у обоих ≥ 2 таблицы.

**Сравниваются только отчёты одного РЦ.** Один и тот же отчёт заведён на
нескольких заводах — это нормальное устройство, а не дубль: объединять там
нечего, а межзаводских пар набегает столько, что за ними не видно настоящих
кандидатов. Сравнение идёт через `COALESCE`, а не напрямую: `NULL = NULL` в
SQL неверно, и отчёты без указанного завода выпали бы из сравнения совсем.
Ограничение по РЦ заодно делает ненужным отбор по совпадению имени и
каталога: внутри одного завода пара «имя + каталог» уникальна по ключу
отчёта.

Страница **«Похожие отчёты»** (раздел «Отчёты») не читает эту витрину
напрямую — у неё свой запрос с тем же принципом (только TABLE, только внутри
одного РЦ), но с порогом сходства, управляемым ползунком, и с разбором
конкретной пары по щелчку: какие таблицы общие, какие есть только у одного из
отчётов. Порог «не меньше 2 общих таблиц» обязателен и в этом запросе — без
него любые два отчёта, использующие общий справочник вроде календаря, попали
бы в кандидаты, а таких пар на реальных данных могут быть десятки тысяч.

### 5.8. `v_report_twin` и `v_network_report_twin` — двойники отчёта

Зеркало 5.7. Там пары ищутся **внутри** одного завода, потому что один и тот же
отчёт на нескольких площадках — норма, а не дубль. Здесь эта норма и есть
предмет вопроса: что у сети (или у завода) есть своё.

**Областей сравнения две, и обе нужны.** На данных заказчика сети пересекаются
по отчётам примерно на 70%, и главный вопрос — чем они **различаются**; внутри
одной сети уникальных отчётов по заводам заметно меньше. Поэтому область не
зашита в витрину, а стоит в колонке `scope`:

| `scope` | С чем сравнивается отчёт |
| --- | --- |
| `NETWORK` | с отчётами **других ТС** (любой их завод) |
| `PLANT` | с другими заводами **своей ТС** |

`v_report_twin` — строка на «отчёт × область», и в ней два независимых факта о
двойниках:

- `name_twin_count`, `name_twin_where` — тёзки: в области `NETWORK` считаются и
  перечисляются сети, в области `PLANT` — заводы. Сравнение по
  `LOWER(TRIM(...))`: регистр и краевые пробелы в выгрузке разъезжаются, а отчёт
  при этом один и тот же;
- `best_jaccard`, `best_shared_tables`, `best_twin_report`, `best_twin_where` —
  ближайший по набору **настоящих таблиц** отчёт на другой стороне и величина
  сходства. `NULL` — такого отчёта нет вовсе.

Порог «не меньше 2 общих таблиц» тот же, что в 5.7: одна общая таблица означает
общий справочник вроде календаря, а не тот же отчёт. Пара обязана быть между
разными РЦ — внутри одного завода похожие отчёты ищет 5.7, и смешивать эти два
вопроса нельзя. `COALESCE`, а не сравнение напрямую: `NULL = NULL` в SQL неверно,
и отчёты без заполненной сети или завода выпали бы из сравнения совсем.

`counterparts_compared` говорит, было ли вообще с чем сравнивать: ноль означает,
что других сетей (или заводов в этой ТС) с отчётами нет, и «все отчёты
уникальны» — это не вывод, а отсутствие данных.

**`v_network_report_twin` — отчёт сети целиком:** строка на «ТС + наименование»,
заводы схлопнуты. Для вопроса «чем различаются сети» единица счёта именно такая:
отчёт, стоящий на трёх заводах одной ТС, — это один отчёт этой сети, и считать
его трижды значило бы завысить «своё» втрое. Таблицы берутся объединением по
заводам сети (одна и та же таблица на трёх заводах остаётся одной), объём и
запуски — суммой (на каждом заводе отчёт читает свои данные и запускается своё
число раз). Двойник в других ТС берётся по худшему для уникальности случаю: если
хотя бы одна площадка отчёта нашла двойника в другой сети, значит отчёт в других
сетях есть.

**Витрины не решают, уникален ли отчёт.** Они отдают факты, а порог сходства
задаёт читатель — на странице это ползунок. Зашить порог внутрь значило бы иметь
две разные правды об уникальности: одну в SQL, другую на странице.

---

## 6. Компоненты и структура репозитория

```
ReportsDB/
├── README.md, README.en.md    Быстрый старт для коллеги (двуязычно)
├── docs/TZ.md, TZ.en.md       Этот документ
├── docs/RUNBOOK.md, ARCHITECTURE.md  + версии .en.md
├── config/mapping.yml         Маппинг колонок Excel → модель (ЕДИНСТВЕННОЕ место)
├── data/
│   ├── raw/                   Входные Excel-файлы (в git не попадают)
│   └── reports.duckdb         Результат ETL (в git не попадает)
├── sql/
│   ├── 01_schema.sql          DDL
│   └── 02_views.sql           Витрины
├── src/reportsdb/
│   ├── cli.py                 Точка входа: profile | build | export | sample
│   ├── config.py              Загрузка mapping.yml, пути
│   ├── profile_source.py      Профилирование Excel: листы, заголовки, примеры
│   ├── etl.py                 Загрузка и нормализация
│   ├── export_html.py         Сборка самодостаточного HTML
│   ├── diagnose.py            Сверка файла размеров с базой
│   └── sample_data.py         Генератор синтетических данных
├── app/Home.py, app/views/    Streamlit-приложение (pages/ не годится: нет вложенных разделов)
├── docker/                    Dockerfile, docker-compose.yml
├── scripts/run.sh, run.bat    Запуск в одну команду
└── pyproject.toml
```

---

## 7. ETL: требования к поведению

0. **Версия структуры.** При любом изменении `sql/01_schema.sql` или
   `sql/02_views.sql` обязательно поднять `SCHEMA_VERSION` в
   `src/reportsdb/config.py` и обновить слепок в
   `test_schema_version_matches_view_set`. Иначе у пользователей останется
   несовместимая база, а приложение не сможет об этом предупредить.
1. **Идемпотентность.** `build` всегда пересоздаёт `reports.duckdb` с нуля.
   Существующий файл переименовывается в `reports.duckdb.bak` (одна копия).
1.1. **Несколько файлов на роль.** Выгрузка приходит по файлу на завод, поэтому
   `build` принимает список файлов отчётов, а `table_sizes.files` и
   `report_usage.files` — списки. Файлы **склеиваются до разбора**, а не
   грузятся по очереди: сквозная нумерация `report_id` и `table_id` ведётся по
   всему набору, и загрузка по одному файлу выдавала бы одинаковые ключи для
   разных строк. Завод берётся из колонок ТС и Завод внутри файла, а не из
   того, какой это файл, — иначе размеры разных заводов молча сложились бы в
   один. Если файлов размеров несколько, а колонки завода в них нет,
   выставляется `stats.size_files_without_plant`: падения не будет, но цифры
   неверные, и промолчать об этом нельзя. Догрузка одного завода к собранной
   базе намеренно не поддержана — она порождает состояние «половина заводов
   свежая, половина старая», о котором по базе не догадаться.
2. **Разбор списка таблиц.** Разделитель `;` (настраивается). Пустые элементы
   отбрасываются. Элемент без точки → сначала попытка восстановить схему по
   файлу размеров (см. п. 2.1); если не вышло — `schema_name = '(unknown)'`,
   `schema_source = 'не определена'`, `is_parsed_ok = FALSE`. Элемент с двумя
   и более точками → берутся два последних сегмента (случай
   `db.schema.table`), остальное отбрасывается.
2.1. **Восстановление схемы по файлу размеров.** До загрузки отчётов по файлу
   размеров строится индекс «имя таблицы → схема» — только для имён, у
   которых в файле встречается ровно одна схема. Ссылка без схемы, чьё имя
   попало в индекс, получает эту схему и `schema_source = 'файл размеров'`;
   неоднозначные имена (несколько схем сразу) не восстанавливаются. Индекс
   строится один раз, до `_load_reports`, поэтому не зависит от того, в каком
   порядке отчёты и файл размеров загружаются в базу.
3. **Нормализация пути каталога.** Обратные слэши → прямые, схлопывание
   повторов, ведущий `/`, без завершающего `/`. Пустой путь → `/`.
4. **Отбраковка.** Строка без имени отчёта попадает в `etl_reject` и не
   прерывает загрузку. Строка без таблиц загружается — отчёт без источников
   валиден и интересен аналитике.
5. **Отчёт о загрузке.** В stdout: число строк прочитано / загружено /
   отброшено, число уникальных таблиц, число неразобранных элементов.
6. **Кодировка и типы.** Все текстовые поля читаются как строки — недопустимо,
   чтобы pandas превратил `1.2` в имя таблицы или потерял ведущие нули.

---

## 8. Streamlit-приложение (вариант A)

Меню собрано разделами через `st.navigation` — «Данные», «Аналитика РЦ»,
«Отчёты», «Справочники», «Инструменты». Каждое из пяти основных представлений
получило **отдельную страницу** внутри раздела «Аналитика РЦ»: вкладки внутри
одной страницы прятали содержимое и не давали ссылаться на конкретный разрез.

Два правила, общие для всех страниц:

- **Выбор РЦ живёт в `session_state`** и не сбрасывается при переходе между
  страницами раздела.
- **Поиск — одним полем сразу по нескольким колонкам**: заказчик должен
  находить строку и по наименованию таблицы, и по наименованию отчёта, не
  выбирая заранее, что именно ищет (`search_box` в `app/_shared.py`).
- **Значений в поиске может быть несколько** — через запятую, точку с запятой
  или каждое с новой строки; строка попадает в результат, если подходит хотя
  бы под одно (ИЛИ, а не И: пересечение по разным таблицам всегда пусто).
  Пробел разделителем не служит — имена отчётов состоят из нескольких слов.
  Поле — `st.text_area`, а не `st.text_input`: в однострочное поле список из
  Excel не вставить, браузер по стандарту вырезает переводы строк, и колонка
  слипается в одно слово. Поиск идёт подстрокой (`regex=False`): заказчик
  вставляет настоящие имена, `[dbo].[Orders]` как регулярка нашла бы совсем
  не то, а одинокая `*` роняла страницу. Ненайденные значения перечисляются
  подписью — при вставке списка это главное, что нужно знать.

Страницы:

0. **Загрузка данных** — единственный путь записи в базу через интерфейс.
   Файлы берутся из `data/raw/` или перетаскиваются на страницу (сохраняются
   туда же). Пользователь указывает роль каждого файла и нажимает «Загрузить».
   Отдельная кнопка собирает HTML для коллег.

   Проверка колонок идёт **по каждому выбранному файлу** — отдельной вкладкой
   на файл. Для каждого ожидаемого поля показано, найдена ли колонка и что
   именно перестанет работать без неё; список потерь собирается в один блок.
   Блокирует загрузку только отсутствие имени отчёта; отсутствие `SIZE_MB` в
   файле размеров блокирует его использование. Отсутствие `SEGMENT_TYPE` даёт
   предупреждение: без него индексы попадут в размер таблиц и завысят его.

   Перед пересборкой приложение обязано закрыть соединения с БД
   (`release_db()`): под Windows занятый файл нельзя ни переименовать, ни
   перезаписать. После пересборки кэши сбрасываются, иначе страницы аналитики
   продолжат показывать прежние данные.

   **Очистка базы** — свёрнутый блок сразу под текущим состоянием, до всех
   `st.stop()` страницы: ниже кнопка просто не отрисовалась бы при пустой
   папке или невыбранном файле. Стирает все данные и оставляет **пустую базу
   нужной структуры**, а не удаляет файл: страницы аналитики продолжают
   открываться и показывают нули вместо ошибки «база не найдена», а версия
   структуры остаётся текущей. Резервная копия `.bak` удаляется вместе с
   базой — очистку просят, когда рабочих данных на машине быть не должно, и
   копия рядом сохранила бы ровно то, что просили стереть; флажок оставляет
   её при необходимости. Исходные файлы в `data/raw/` не трогаются, и
   страница прямо говорит, сколько их там осталось. Защита от случайного
   нажатия — свёрнутый блок и обязательный флажок подтверждения.

1. **Обзор** — главный дашборд, всё в разрезе выбранного РЦ, выбор в шапке,
   выбранная пара «сеть + завод» вынесена в подзаголовок. Сверху вниз:

   - **строка плиток**: отчётов, таблиц, объём таблиц (ГБ), схем БД. Таблицы и
     объём берутся из каталога №1, то есть из файла размеров, и считаются по
     таблицам, а не по строкам «таблица + завод»;
   - **три круговых разреза** по три доли: объём по использованию (несколько
     отчётов / один / не используются), глубина хранения, обращения к отчётам.
     Больше трёх долей нельзя — см. журнал решений;
   - **справочные таблицы**: отчёты по каталогам первого уровня (столбики
     одним цветом) и схемы БД **таблицей**. Схемы именно таблицей: их много,
     столбики уезжали далеко вниз, а у части схем размер неизвестен — на
     диаграмме это было пустое место. Схемы без размера в таблицу не
     попадают, их число указано подписью;
   - **качество исходных данных** (отбраковка, ссылки без схемы). РЦ не
     касается: это свойство загрузки, а не площадки.

   Высота таблиц считается по числу строк (`table_height()`) — фиксированная
   высота у короткой таблицы оставляет полосу пустых строк.

1.1. **№1 «Таблицы и размеры»** — единственная страница раздела «Аналитика РЦ»,
   которая **не** подчиняется выбору РЦ в шапке: это самостоятельный каталог
   таблиц из файла размеров и только из него. Строка — пара «таблица + завод».
   Свои фильтры: сеть, завод, связь с отчётами («все» / «используются
   отчётами» / «не используются»), поиск по имени. KPI: строк
   «таблица + завод», уникальных таблиц, суммарный объём, сколько таблиц не
   использует ни один отчёт. Под таблицей — подпись, сколько ссылок отчётов
   указывает на таблицы вне файла размеров: в список они не добавляются, но
   промолчать о них нельзя, у таких отчётов объём занижен.

1.5. **Карточка отчёта** — список отчётов строками-кнопками: щелчок в любом
   месте строки разворачивает полную картину, повторный щелчок её сворачивает.
   Видно, какие таблицы использует отчёт и сколько каждая весит, сколько раз он
   запускался, его view, временные объекты и процедуры, текст SQL-запроса (если
   загружен файл `report_sql`) на отдельной вкладке, а также отчёты-соседи,
   которые используют те же таблицы. Сортировка — отдельным полем, полная
   таблица с сортировкой по колонкам и выгрузкой — в свёрнутом блоке.

1.6. **№2 «Отчёт → таблицы»** — два вида одной связи. «По отчётам»: строка —
   отчёт, видно из каких таблиц он состоит. «По таблицам»: строка — таблица,
   видно в какие отчёты она попадает. Щелчок по строке в любом из видов даёт
   разбор: показатели строки и её таблицы-источники (или отчёты-потребители).
   В виде «по таблицам» объём берётся как максимум по строкам таблицы, а не
   как сумма: одна таблица приходит там в нескольких отчётах.

2. **Объём и стоимость** ⭐ — построена вокруг вопроса «на какие таблицы
   ссылается отчёт и сколько они весят суммарно»: витрина
   `v_report_tables_summary` даёт и число таблиц, и их перечень, и сумму
   размеров одной строкой. Две вкладки по двум метрикам стоимости:
   «Объём данных» (топ-20 по `exclusive_mb`, скаттер «объём против частоты») и
   «Время выполнения» (топ-20 по суммарному времени, распределение по
   длительности выборки). Фильтры: сеть, завод, три уровня каталога, работа
   через view, покрытие размерами. Уровни 2 и 3 — каскадом от верхнего.
3. **Таблицы** — справочник **всех объектов-источников**, а не только таблиц:
   `v_table_criticality`, поиск, фильтр по типу объекта русскими названиями,
   флажок «только те, что не используются отчётами». Сверху — разбивка «каких
   объектов сколько» по типам: без неё не видно, что представления в данных
   вообще есть. В таблице есть колонка «Тип определён» — колонка файла / маска
   имени / файл размеров / по умолчанию.
4. **Кандидаты на вывод** — `v_decommission_candidates` с фильтром по
   `confidence`, выгрузка в CSV.
4.1. **Похожие отчёты** — пары отчётов с похожим или совпадающим набором
   таблиц (см. 5.7), порог сходства регулируется ползунком, щелчок по паре
   раскрывает список общих и различающихся таблиц.

4.2. **Уникальные отчёты** — обратная сторона 4.1: отчёты, у которых на другой
   стороне нет ни тёзки, ни близкого по набору таблиц двойника (см. 5.8).
   Область сравнения выбирается переключателем, потому что вопросов два:
   «чем различаются сети» (сети пересекаются по отчётам примерно на 70%, и
   различия — главное, что нужно видеть) и «что есть только на этом заводе»
   внутри одной ТС. Вместе с областью меняется единица счёта: отчёт сети
   (заводы схлопнуты) или запись отчёта на заводе. Свод «отчётов всего /
   уникальных / доля / объём их таблиц / запуски», стопка «уникальные и есть на
   другой стороне», ползунок порога сходства и щелчок по отчёту — его таблицы с
   объёмом и глубиной хранения, запуски и объяснение, почему отчёт уникален:
   ближайший отчёт другой стороны назван вместе с величиной сходства. Сеть или
   завод, которым не с кем сравниться, названы прямо: «все отчёты уникальны»
   там означает, что сравнивать было не с чем, а не что отчёты особенные.

4.3. **ABC-анализ** — отчёты, отсортированные по выбранной мере и разрезанные
   по накопленной доле: A до 80%, B до 95%, C — хвост. Плитки по группам,
   кривая накопления с опорными линиями 80/95%, круговая по долям групп,
   таблица с фильтром по группе. Диаграмму Парето рисовать нельзя: она кладёт
   столбцы и кривую на две разные оси.

   Мер четыре, и порядок неслучаен. Первая — «Объём таблиц отчёта, МБ»
   (`gross_mb`): сколько данных отчёт читает; есть почти у каждого и читается
   без оговорок. «Освободится при выводе, МБ» (`exclusive_mb`) вторая: она
   считается только по таблицам, которых не касается больше никто, и у отчёта
   на общих таблицах честно равна нулю — такой отчёт в анализ по этой мере не
   попадает вовсе. Дальше «Число обращений» и «Суммарное время выполнения».

   Под выбором меры выводится её определение, а под плитками — **сколько
   отчётов в анализ не вошли и почему**. Без этой строки главный вопрос
   читателя «почему список короче, чем отчётов в базе» остаётся без ответа.

5. **Сети и заводы** — сравнение РЦ между собой (см. 5.5). Плитки: сетей,
   заводов, объём базы, доля под отчётами. Ниже — стопка по каждому заводу
   «под отчётами / без отчётов» (две величины одной природы и одной шкалы —
   честные столбики, а не две оси) и таблица сравнения. Строки-заглушки
   «(не указана)/(не указан)» в счётчики сетей и заводов не идут: это не сеть
   и не завод, а незаполненная колонка.

5.1. **Запросы к БД** (раздел «Инструменты») — текст SQL, которым формируется
   отчёт. Отдельная страница, а не только вкладка в карточке отчёта, потому что
   вопрос обратный: карточка отвечает «всё про этот отчёт», а здесь ищут **по
   тексту запроса**. Поиск идёт и по `sql_text`, поэтому «какие отчёты трогают
   эту таблицу» отвечается здесь даже для таблиц, не попавших в колонку
   «Таблицы источники данных». Плитки: отчётов с запросом, их доля, медиана и
   максимум строк в запросе (медиана, а не среднее — один запрос на тысячу
   строк перекосил бы среднее). Щелчок по отчёту показывает запрос с подсветкой
   синтаксиса, выгрузку в `.sql` и **заявленные источники рядом**, с отметкой
   «есть в запросе»: расхождение между списком источников из файла и текстом
   запроса — то, что эта страница помогает заметить. Сравнение по имени и без
   схемы тоже, потому что в запросах таблицу пишут и так, и так; «нет» бывает
   законным — объект может приходить через view или процедуру.

5.2. **Файл для коллег** (раздел «Инструменты») — сборка самодостаточного
   HTML. Отдельная страница, а не блок внизу загрузчика: файл собирают не в
   тот же момент, когда грузят данные, а на странице загрузки он к тому же
   оказывался за `st.stop()` и просто не отрисовывался, пока файлы не выбраны.
   Показывает, что попадёт внутрь, дату и размер уже собранного файла и
   напоминает, что внутри рабочие данные. Кнопка скачивания живёт вне
   обработчика нажатия — иначе она исчезала бы при первом же перезапуске
   страницы, который сама и вызывает.

6. **SQL** — произвольный запрос к БД с ограничением 5000 строк и выгрузкой CSV.
   Сверху — справочник структуры: каждая таблица и витрина с назначением, под
   ней список полей с типами и пояснением к неочевидным. Колонки читаются из
   `information_schema`, а описания лежат словарём в коде, поэтому справочник
   не разъезжается со схемой. Там же — два правила, без которых запрос даст
   неверные цифры: соединять отчёт с размером только через
   `v_report_table_size` и не складывать `gross_mb` по отчётам.

Требования: работа полностью офлайн (без CDN), кириллица в подписях,
единая палитра, все таблицы выгружаются в CSV, единый вид интерфейса
(`inject_theme()` в `app/_shared.py`) — карточки-показатели, мягкие рамки
таблиц, приглушённые заголовки разделов меню, одинаково в светлой и тёмной
теме.

---

## 9. Самодостаточный HTML (вариант B)

Один файл `dist/reportsdb.html`. Внутри: данные как JSON, стили и скрипты
инлайном, ноль внешних запросов. Обязан открываться по `file://` в Chrome, Edge,
Firefox.

**Состав повторяет разделы приложения.** Коллега без Python и Docker обязан
видеть то же, что владелец базы: вкладки собраны по тем же группам меню, и в
каждой те же показатели, те же колонки и тот же разбор выбранной строки.

| Группа вкладок | Вкладки |
| --- | --- |
| Данные | Обзор: четыре плитки в разрезе РЦ, сравнение РЦ, схемы БД таблицей, топ-20 по освобождаемому объёму |
| Аналитика РЦ | №1 Таблицы и размеры · №2 Отчёт → таблицы · №3 Отчёт → функции · №4 Отчёт → обращения · №5 Глубина хранения |
| Отчёты | Карточка отчёта · Объём и стоимость · Кандидаты на вывод · Похожие отчёты · ABC-анализ |
| Справочники | Объекты-источники: таблицы, view, mat.view, временные, процедуры |

Общие свойства:

- **Выбор РЦ один на весь файл** — как в приложении. Плитки показателей
  считаются по тому, что реально показано, а не по всей базе.
- **№2 разделён на два вида**, «По отчётам» и «По таблицам», плюс полный
  список связей; щелчок по строке в любом из них разворачивает разбор.
- **Разбор строки** есть там же, где в приложении: карточка отчёта, оба вида
  №2, пара в «Похожих отчётах». Повторный щелчок снимает выбор.
- **Никаких `LIMIT`** в запросах выгрузки: обрезка молча показывала бы меньше,
  чем приложение. Единственный отбор — содержательный порог сходства в
  «Похожих отчётах».
- Сортировка, поиск, фильтры, выгрузка текущей выборки в CSV, светлая и тёмная
  тема. Целевой размер — до 5 МБ.

**Чего в файле нет:** загрузки данных, произвольного SQL и диаграмм Plotly —
это инструменты владельца базы, а не читателя. Диаграммы, которые остались,
нарисованы на чистом CSS.

---

## 10. Доставка коллегам

- **Docker:** `docker compose -f docker/docker-compose.yml up` → Streamlit на
  `http://localhost:8501`. Образ содержит и код, и собранную БД.
- **Браузер:** файл `dist/reportsdb.html` по почте.
- **Python:** `scripts/run.sh` / `run.bat` — создают venv через `uv` и стартуют.

---

## 11. Порядок работы агента при появлении реального Excel-файла

0. Убедиться, что `data/raw/` под `.gitignore` (см. 3.1.1). Файл заказчика
   никуда не выкладывается.
1. Положить файл в `data/raw/`.
2. `python -m reportsdb profile data/raw/<файл>` — вывести листы, заголовки,
   по 5 примеров значений, долю пустых.
3. Заполнить `config/mapping.yml` по факту. Ничего не угадывать: если смысл
   колонки неясен — спросить заказчика.
4. `python -m reportsdb build` — собрать БД. Проверить сводку и `etl_reject`.
5. Прогнать `tests/` — контрольные проверки целостности.
6. `python -m reportsdb export-html` — собрать HTML.
7. Обновить раздел 15 и запушить.

---

## 12. Контроль качества

Проверки в `tests/test_integrity.py`, запускаются на синтетических данных и на
реальных:

- у каждой связи в мосте существуют обе стороны (нет висячих ключей);
- `full_name` в `dim_table` уникален;
- сумма `exclusive_mb` по всем отчётам ≤ сумме `total_mb` по всем таблицам —
  ключевая защита от двойного учёта;
- число строк исходника = загружено + отброшено;
- все витрины выполняются на пустых фактах и возвращают результат без ошибок;
- **каждое загруженное поле присутствует хотя бы в одной витрине** — иначе
  колонку прочитали впустую (`test_every_loaded_field_is_visible_in_analytics`);
- `total_duration_sec` равен `exec_count × avg_duration_sec`;
- `SCHEMA_VERSION` записана в базу, а слепок SQL сверяется с ней: правка схемы
  без поднятия версии роняет тест;
- таблица №1 содержит **ровно** строки файла размеров: ни потерь, ни добавок
  из отчётов, и ни одна пара «таблица + завод» не показана дважды; №2 содержит
  только `TABLE`, №3 только `ROUTINE`, границы глубины 30/45 расставлены верно;
- одна таблица имеет разный размер на разных заводах, а резолвер
  `v_report_table_size` не размножает строки отчёта: число его строк равно
  числу строк `bridge_report_table`, и для отчёта берётся замер его завода;
- фактические заголовки заказчика (`№`, `Наименование отчета`, `Каталог`,
  `Таблицы источники данных`) сопоставляются без правок конфига;
- `.gitignore` закрывает `data/raw/*`, `data/*.duckdb` и `dist/`;
- схема, восстановленная по единственному совпадению имени в файле размеров,
  даёт ровно одну строку `dim_table` (не дублирует запись из файла размеров);
  неоднозначное имя (несколько схем сразу) не восстанавливается и остаётся
  `(unknown)`; `is_parsed_ok` всегда согласован со `schema_source`.

---

## 13. Ограничения и риски

| Риск | Смягчение |
| --- | --- |
| Список таблиц в Excel неполон или устарел (заполнялся руками) | В UI явная оговорка: анализ отражает содержимое файла, а не фактические зависимости. Достоверный источник — парсинг `.rdl` или `ReportServer.ExecutionLog`. |
| Двойной учёт общих таблиц | Введён `exclusive_mb`; `gross_mb` помечен как небезопасный для суммирования. |
| Неполные данные о размерах | `size_coverage_pct` и понижение `confidence`. |
| Отчёт может тянуть данные через хранимые процедуры и вьюхи | Фиксируется как известное ограничение; при появлении данных добавить `object_type` в `dim_table`. |

---

## 14. Определение готовности

- [x] Схема БД и витрины созданы, работают на пустых фактах.
- [x] ETL собирает БД из Excel по конфигу, ведёт журнал и отбраковку.
- [x] Streamlit-приложение запускается локально и в Docker.
- [x] Самодостаточный HTML открывается по `file://`.
- [x] Синтетические данные позволяют проверить всё до появления реального файла.
- [x] Тесты целостности проходят.
- [x] Маппинг подтверждён под фактические заголовки заказчика.
- [x] Загрузка через интерфейс: выбор файлов, предпросмотр, кнопка «Загрузить».
- [x] Проверено на реальном файле заказчика (локально, вне репозитория).
- [x] Поддержана структура файла размеров (выгрузка сегментов БД).
- [x] Частота использования и длительность загружаются отдельным файлом
      (`report_usage`); в основном файле эти колонки не читаются.
- [x] Текст SQL-запроса отчёта загружается отдельным файлом (`report_sql`) и
      виден в карточке отчёта.
- [ ] Загружен реальный файл размеров.

---

## 15. Журнал решений

Только дополняется. Каждая запись — решение и причина за ним: ради причины
журнал и ведётся. Новые записи добавляются в **обе** языковые версии одним
коммитом.

| Дата | Решение |
| --- | --- |
| 2026-07-30 | Зафиксированы стек (DuckDB + Streamlit + HTML), режим загрузки (full refresh), приоритет аналитики (объём и стоимость). |
| 2026-07-30 | Вариант B реализован на встроенном JSON + JS вместо DuckDB-WASM — см. 2.1. |
| 2026-07-30 | Введено разделение `gross_mb` / `exclusive_mb` для защиты от двойного учёта общих таблиц. |
| 2026-07-30 | Каркас проекта реализован и проверен на синтетических данных; ожидается реальный Excel-файл. |
| 2026-07-30 | Заказчик подтвердил фактические заголовки: `№`, `Наименование отчета`, `Каталог`, `Таблицы источники данных`. Маппинг и генератор синтетики приведены к ним, добавлено поле `report_no`. |
| 2026-07-30 | Зафиксировано требование: данные загружаются только локально и в git не попадают (раздел 3.1.1). |
| 2026-07-30 | Добавлена страница «Загрузка данных»: выбор файлов из папки или перетаскивание, предпросмотр сопоставления колонок, пересборка по кнопке. Командная строка перестала быть обязательной. |
| 2026-07-30 | Проверено на реальном файле заказчика: 3 отчёта, 15 таблиц, 16 связей, отбраковки нет. |
| 2026-07-30 | Добавлены `docs/RUNBOOK.md` (пошаговый запуск) и `docs/ARCHITECTURE.md` (компоненты решения). |
| 2026-07-30 | **Новая структура входных файлов.** Каталог пришёл тремя колонками уровней, добавились ТС, завод и признак view; частота и длительность переехали в основной файл. Ключ уникальности отчёта расширен до `(сеть, завод, каталог, имя)`. |
| 2026-07-30 | Файл размеров — выгрузка сегментов БД. Сегменты одной таблицы суммируются, не-табличные типы (индексы, LOB) пропускаются: связать их с таблицей по выгрузке нельзя. |
| 2026-07-30 | Длительность хранится в секундах (`avg_duration_sec`), как в исходном файле. Добавлена вторая метрика стоимости — суммарное время `exec_count × avg_duration_sec`. |
| 2026-07-30 | `uses_view` понижает достоверность кандидата на вывод: за view стоят таблицы вне списка источников. |
| 2026-07-30 | Страница загрузки переработана: проверка колонок по каждому выбранному файлу отдельной вкладкой, для каждого поля указано, что перестанет работать без него. |
| 2026-07-30 | Уровни каталога 2 и 3, `percent_of_total` и `segment_count` доведены до витрин — до этого они загружались, но нигде не показывались. Добавлен тест-страж, который ловит такой разрыв. |
| 2026-07-30 | Введена версия структуры БД (`SCHEMA_VERSION`, колонка `etl_run.schema_version`). База, собранная прежней версией кода, теперь даёт понятное сообщение с инструкцией вместо ошибки SQL. Страница загрузки работает на любой базе — иначе путь восстановления был бы недоступен. |
| 2026-07-30 | Меню перестроено разделами (`st.navigation`), каждое из пяти представлений вынесено на отдельную страницу раздела «Аналитика РЦ». Добавлена карточка отчёта с выбором строки. Во всех таблицах — единый поиск по имени таблицы и отчёта. |
| 2026-07-30 | Добавлена витрина `v_report_tables_summary`: отчёт, перечень его таблиц и их суммарный размер одной строкой — прямой ответ на вопрос «на что ссылается отчёт и сколько это весит». |
| 2026-07-30 | **Пять основных представлений в разрезе РЦ** (раздел 5.6). Введён тип объекта: таблицы, view, mat.view, временные и процедуры приходят отдельными колонками и различаются в модели, поэтому таблица №2 содержит только настоящие таблицы, а №3 — только процедуры. Добавлена глубина хранения на таблицу. РЦ определяется парой «сеть + завод». |
| 2026-07-30 | Маски имён для типа объекта оставлены пустыми по умолчанию: неверно угаданный тип молча выкинул бы таблицу из аналитики. Источник типа сохраняется в `kind_source`. |
| 2026-07-30 | Добавлена витрина `v_report_duration` и вкладка «Время выполнения»: длительность стала полноценной метрикой, а не просто колонкой в таблице. |
| 2026-07-31 | **Размер таблицы ведётся по заводам.** Ключ `fact_table_size` расширен до `(table_id, network, plant)`, в файле размеров появились колонки `ТС` и `Завод`. Одна таблица на разных заводах весит по-разному, и складывать эти замеры в один было неверно. |
| 2026-07-31 | Введена витрина-резолвер `v_report_table_size`: соединять отчёт с размером напрямую нельзя — join к `fact_table_size` размножил бы строки по числу заводов и завысил объём кратно. Резолвер берёт замер завода самого отчёта, при его отсутствии — общий. Все шесть прежних прямых join переведены на неё. |
| 2026-07-31 | **№1 «Таблицы и размеры» отвязана от отчётов** (`v_rc_tables` → `v_tables_catalog`). Список таблиц существует сам по себе: в каталог попадают все строки файла размеров, включая таблицы, к которым не тянется ни один отчёт. `report_count` оставлена как справочная колонка. Загрузчик заводит недостающие `dim_table` с `kind_source = 'файл размеров'`. |
| 2026-07-31 | Строки-кнопки приведены к виду таблицы: моноширинный шрифт, колонки фиксированной ширины, шапка, по 12 строк на страницу. Крупные кнопки-плашки занимали весь экран, и разбор выбранной строки уезжал под сгиб — а смотреть на него надо вместе с таблицей. Шапка сделана выключенной кнопкой: у отдельного `div` не совпадали метрики шрифта, и колонки разъезжались с заголовками. Фильтры собраны в один ряд, выгрузка и полная таблица убраны под разбор. |
| 2026-07-31 | **Выбор строки — щелчком по самой строке.** `st.dataframe` выделяет строку только флажком в левой колонке: щелчок по ячейке он игнорирует (проверено на Streamlit 1.60 — это последняя версия, обхода внутри виджета нет; ссылки в колонках Streamlit принудительно открывает в новой вкладке). Поэтому строка стала кнопкой во всю ширину — помощник `row_picker` в `app/_shared.py`. Выбор хранится по значению ключевой колонки, а не по номеру строки: при смене сортировки номер уезжает. Полная сортируемая таблица осталась в свёрнутом блоке. |
| 2026-07-31 | Обзор переведён в разрез РЦ: выбор сети и завода в шапке, в заголовке видно, что выбрано. Показателей осталось три — отчётов, таблиц, объём таблиц; таблицы и объём берутся из каталога №1, то есть из файла размеров. Доля БД, покрытие размерами, отчёты через view и число связей убраны: они отвечали на вопросы, которых на обзорной странице не задают. |
| 2026-07-31 | №2 разделена на два вида: «По отчётам» (из каких таблиц состоит отчёт) и «По таблицам» (в какие отчёты попадает таблица). Щелчок по строке разворачивает разбор. Показатель «связей отчёт → таблица» убран как техническая величина. |
| 2026-07-31 | На странице SQL появился справочник структуры: список колонок берётся из самой базы, а назначение объектов и смысл неочевидных полей — из словаря в коде, поэтому справочник не разъезжается со схемой. Добавлены готовые запросы под разрез по заводам. |
| 2026-07-31 | Схема таблицы приводится к нижнему регистру при загрузке. Из отчётов она приходит как «dbo», из выгрузки сегментов как «DBO» — сводки по схемам двоились, и половина объёма уходила в схему-двойник. Проверяется `test_schema_names_have_one_case`. |
| 2026-07-31 | Добавлен `.streamlit/config.toml`: цвет выделения — синий из палитры вместо красного по умолчанию. Красным в приложении помечены проблемы, и выбранная строка не должна выглядеть как ошибка. |
| 2026-07-31 | Проверка «пусто» в интерфейсе переведена на `pd.isna`. Приём «значение не равно самому себе» ловит `float('nan')`, но целочисленная колонка с NULL приезжает из DuckDB как nullable `Int32`, где пусто — это `pd.NA`, а `pd.NA != pd.NA` возвращает `pd.NA`: карточка отчёта падала с «boolean value of NA is ambiguous». Помощники `num` и `is_blank` переехали в `app/_shared.py`, чтобы правило было одно на все страницы. Проверяется `tests/test_ui_helpers.py`. |
| 2026-07-31 | Проверка интерфейса дополнена **щелчком по строке**: прежний обход только открывал страницы, поэтому карточка отчёта, которая рисуется лишь после выбора строки, оставалась непроверенной — там и жил сбой. |
| 2026-07-31 | Добавлены обёртки `scripts/rdb.bat` и `rdb.sh`. Пакет ставится в виртуальное окружение проекта, поэтому `python -m reportsdb` из обычной консоли отвечает «No module named reportsdb» — заказчик упёрся в это на первой же команде. Обёртка запускает команду тем же Python, что и приложение; документация переведена на неё. |
| 2026-07-31 | Добавлена команда `diagnose` — сверка файла размеров с базой. Заказчик увидел в №1 больше таблиц, чем в своём файле, и проверить это можно было только на его машине: данные не покидают её. Команда читает файл ровно так же, как загрузчик, и показывает, где расходятся числа. Имена таблиц по умолчанию не печатаются (`--show-names` — локально). |
| 2026-07-31 | Ненайденная колонка типа сегмента перестала быть тихой ошибкой: раньше индексы и LOB молча становились таблицами и завышали и число таблиц, и объём. Теперь загрузка выводит ошибку в сводке и на странице загрузки, строки с пустым типом считаются отдельно. Проверяется тестом `test_missing_segment_type_column_is_reported`. |
| 2026-07-31 | **Файл размеров — единственный источник списка таблиц БД.** Объекты, упомянутые в отчётах, но отсутствующие в файле размеров, в №1 больше не подмешиваются: замыкающий `UNION` с признаком `size_unknown` убран, мост «отчёт ↔ таблица» только ссылается на список. Чтобы такие ссылки не пропали молча, под таблицей выводится их число со ссылкой на №2 и «Покрытие размерами». Показатель «не используются отчётами» переведён на счёт таблиц вместо строк: строк на таблицу столько, на скольких она заводах. |
| 2026-08-01 | Добавлена страница «Похожие отчёты» (раздел «Отчёты»): пары отчётов, ссылающихся на один и тот же набор настоящих таблиц (индекс Жаккара, порог regulируется ползунком), с раскрытием общих и различающихся таблиц по щелчку. Пары, где совпадение объясняется тем, что это один и тот же отчёт на разных заводах (одинаковые имя и каталог), исключены — иначе легитимная многозаводская копия отчёта постоянно попадала бы в «похожие». Порог «не меньше 2 общих таблиц» — иначе любые два отчёта с общим справочником вроде календаря считались бы кандидатом, а таких пар на реальных данных могут быть десятки тысяч. |
| 2026-08-01 | **Восстановление схемы по файлу размеров.** Ссылка на таблицу без схемы (`(unknown)`) больше не остаётся неразрешённой, если имя таблицы встречается в файле размеров ровно с одной схемой — та и присваивается. Раньше такая ссылка и её «настоящая» запись из файла размеров существовали как две разные строки `dim_table` (одна — сирота с `(unknown)`, другая — из файла размеров с настоящей схемой), поэтому размер и глубина хранения такой таблицы не доходили до отчёта. Неоднозначные имена (встречаются сразу под несколькими схемами) не трогаются — так и остаются `(unknown)`, догадка не выдаётся за факт. Источник схемы хранится в новой колонке `dim_table.schema_source` (`ссылка` / `файл размеров` / `не определена`), по тому же принципу, что и `kind_source`; `is_parsed_ok` теперь производная от неё. `SCHEMA_VERSION` поднята до 8. |
| 2026-08-01 | Единый вид интерфейса: карточки-показатели вместо голых чисел, мягкая рамка и скругление у таблиц, приглушённые заголовки разделов меню, тонкая черта под заголовком страницы. Все цвета — оттенком поверх текущего фона (`rgba`, не hex-код), поэтому выглядит одинаково в светлой и тёмной теме — проверено переключением обеих. Один инжектор `inject_theme()` в `app/_shared.py`, вызывается один раз из `Home.py`. Селекторы (`data-testid`) сверены с фактическим DOM через Playwright, а не угаданы по документации — Streamlit их не документирует и меняет между версиями. |
| 2026-08-01 | **Тёмная тема по умолчанию.** `base = "dark"` в `.streamlit/config.toml`, фон `#0f1419`. Палитра диаграмм переведена на тёмные ступени той же эталонной палитры (`#3987e5` / `#d95926` / `#199e70`) — это не осветление светлых значений, а отдельно подобранные под тёмный фон ступени; проверено `scripts/validate_palette.js --mode dark --surface #0f1419 --pairs all`, все проверки пройдены. `backgroundColor` в конфиге и `SURFACE` в `app/_shared.py` обязаны совпадать: этим цветом рисуются зазоры между сегментами диаграмм. |
| 2026-08-01 | **Круговые диаграммы — строго три доли.** В круге читатель сравнивает все доли между собой, поэтому проверка идёт в режиме `--pairs all`. Четвёртый цвет палитры её не проходит: жёлтый и оранжевый неразличимы даже при полном цветовосприятии (ΔE 10.6 при пороге 15), а перебор всех четвёрок дал только два прошедших набора, и оба ломают фиксированный порядок палитры. Поэтому помощник `donut()` в `app/_shared.py` отказывается рисовать больше трёх долей: хвост сворачивается в «Прочие», а где классов больше — столбики одним цветом. Доли подписаны процентами прямо на диаграмме: идентичность не держится на одном цвете. |
| 2026-08-01 | Обзор перестроен как дашборд: строка плиток (отчётов, таблиц, объём, схем), три круговых разреза («объём по использованию», «глубина хранения», «обращения к отчётам»), ниже справочные таблицы. Схемы БД показаны таблицей, а не столбиками: схем много, диаграмма уезжала далеко вниз, и у части схем размер неизвестен — на диаграмме это было пустое место. Высота таблиц считается по числу строк (`table_height()`): фиксированная высота у короткой таблицы оставляла полосу пустых строк. |
| 2026-08-01 | Добавлена страница «ABC-анализ отчётов»: отчёты сортируются по выбранной мере (объём / обращения / время) и режутся по накопленной доле — A до 80%, B до 95%, C — хвост. Диаграмма Парето намеренно не рисуется: классический Парето кладёт столбцы и кривую на две разные оси, а две шкалы в одних координатах читаются неверно. Вместо неё одна кривая накопления с опорными линиями 80/95%, величины по отчётам — в таблице. Первый отчёт всегда попадает в A: если он один даёт больше 80%, условие «накоплено ≤ 80» его бы не поймало и группа A осталась бы пустой. |
| 2026-08-01 | **Несколько файлов на роль — по файлу на завод.** `build` принимает список файлов отчётов, `table_sizes.files` и `report_usage.files` — списки; в интерфейсе выбор файлов стал множественным. Файлы склеиваются до разбора, а не грузятся по очереди: нумерация `report_id` и `table_id` сквозная по всему набору, и загрузка по одному файлу выдавала бы одинаковые ключи разным строкам. Завод берётся из колонок ТС и Завод внутри файла — привязка «файл = завод» отвергнута: она молча ломается при склейке или повторном выборе файла. Несколько файлов размеров без колонки завода — отдельное предупреждение: строки лягут в «(не указан)», размеры заводов сложатся в один, и никакой ошибки при этом не будет. Догрузка одного завода намеренно не сделана: полная пересборка не оставляет вопроса «что в базе свежее». В конфиге по-прежнему допустим и `file:` (один файл), и `files:` (список) — старые конфиги работают без правок. Проверено тестом, что нарезка демо-данных по четырём заводам даёт базу, идентичную загрузке одним файлом. |
| 2026-08-01 | Сборка HTML-файла для коллег вынесена в отдельную страницу «Инструменты → Файл для коллег». На странице загрузки кнопка стояла в самом низу и после перехода на множественный выбор файлов перестала отрисовываться вовсе: при пустом выборе страница обрывается на `st.stop()` раньше неё. Заодно страница показывает, что попадёт внутрь файла, дату и размер уже собранного, и предупреждает, что внутри рабочие данные. Кнопка скачивания вынесена из обработчика нажатия — иначе она исчезает при первом же перезапуске страницы, который сама и вызывает. |
| 2026-08-02 | **Выгрузка для коллег приведена к составу приложения.** Прежний файл показывал четыре таблицы из десяти страниц «Аналитика РЦ» и «Отчёты», часть данных резалась `LIMIT`, а показателей над таблицами не было вовсе. Теперь вкладки собраны по тем же группам меню, в каждой те же плитки, те же колонки и тот же разбор выбранной строки. `LIMIT` убраны отовсюду: молчаливая обрезка показывала бы коллеге меньше, чем видит владелец базы. Единственный отбор — содержательный порог сходства в «Похожих отчётах». Все связи «отчёт → объект» приходят одним набором, из него собираются №2, №3 и карточка: имя отчёта в строках связей не дублируется, его подставляет JS по `report_id` — на тысячах связей это разница в разы по размеру файла. |
| 2026-08-02 | Число таблиц в карточке бралось из `v_report_footprint.table_count`, который считает **все** объекты отчёта, включая view и процедуры: плитка показывала «Таблиц 8» рядом с разделом «Таблицы (4)». Приложение везде берёт `v_report_tables_summary` — выгрузка переведена на него же, вместе с суммами объёма. Расхождение проверено щелчком по каждой из карточек, а не по первой. |
| 2026-08-02 | Плитки показателей в выгрузке считаются в браузере по тому, что реально показано после разреза по РЦ, поиска и фильтра, а не запросом по всей базе. Иначе при выборе РЦ числа сверху остались бы общими и противоречили бы таблице под ними. Из запросов выгрузки остался только источник данных для подписи в шапке. |
| 2026-08-02 | Выбранная строка в выгрузке хранится значением ключа, а не ссылкой на объект: виды, которые пересчитываются на каждой перерисовке (оба вида №2, ABC), каждый раз дают новые объекты, и сравнение по ссылке теряло бы выделение. Если строка уехала из выборки после смены разреза — выбор снимается сам, чтобы разбор под таблицей не остался о другом отчёте. |
| 2026-08-02 | **Похожие отчёты ищутся только внутри одного завода.** Прежде пары считались по всей базе, а от межзаводских дублей защищал отбор по совпадению имени и каталога — он ловил лишь точные копии. Отчёт, заведённый на нескольких заводах и с тех пор чуть разошедшийся в названии или каталоге, всё равно попадал в «похожие», хотя объединять там нечего: это одна и та же вещь на разных площадках, нормальное устройство, а не дубль. Теперь пара обязана быть внутри одного РЦ (`COALESCE(network) = COALESCE(network) AND COALESCE(plant) = COALESCE(plant)` — сравнение напрямую отбросило бы отчёты без завода, так как `NULL = NULL` в SQL неверно). Прежний отбор по имени и каталогу удалён как избыточный: внутри одного завода пара «имя + каталог» уникальна по ключу отчёта. Правило продублировано во всех трёх местах, где живёт этот запрос: витрина `v_report_overlap`, страница «Похожие отчёты» и выгрузка для коллег. На странице появился выбор РЦ, колонки «Завод 1» и «Завод 2» схлопнуты в одну, показатель «разных заводов» заменён на «заводов с парами». На демо-данных пар стало 2 вместо 9. `SCHEMA_VERSION` поднята до 9. |
| 2026-08-02 | **ABC-анализ: понятные меры вместо одной непонятной.** Мерой по умолчанию был `exclusive_mb` — «объём данных (эксклюзивный)». Он считается только по таблицам, которых не касается ни один другой отчёт, поэтому у отчёта на общих таблицах он равен нулю: в анализ попадали 69 отчётов из 121, а итог составлял 11 301 МБ при объёме базы 170 279 МБ. Цифры верные, но без объяснения выглядят как ошибка. Мер стало четыре, первая — «Объём таблиц отчёта, МБ» (`gross_mb`): сколько данных отчёт читает, есть почти у каждого и читается без оговорок. «Освободится при выводе, МБ» осталась второй с прямым пояснением, почему у многих ноль. Под выбором меры выводится её определение, в плитках появились «Отчётов в анализе» и «Итого», а под ними — сколько отчётов в анализ **не** вошли и по какой причине. Русское согласование числительных вынесено в `plural()`: «23 отчёта», а не «23 отчётов». |
| 2026-08-02 | «Сироты» переименованы в «Не используется отчётами» во всех местах интерфейса: колонка, фильтр, плитка, подсказка на странице SQL. Термин из модели данных не должен быть тем словом, которым интерфейс разговаривает с заказчиком. Имя колонки в базе (`is_orphan`) оставлено: оно короткое, общепринятое и объяснено в справочнике структуры на странице SQL. |
| 2026-08-02 | **Маска `v_` включена: представления видно отдельно от таблиц.** До сих пор `object_patterns` были пусты, и объект без явной колонки типа считался таблицей — в справочнике весь список был одного типа «TABLE». Теперь `VIEW: ["V_*", "VW_*"]` включена по умолчанию, но с защитой: маска не применяется к объектам, найденным в файле размеров. Наличие сегментов означает физическое хранение — это таблица, как бы она ни называлась, и без защиты она выпала бы из таблицы №2 и из расчёта объёма. Ради этого `_schema_recovery_index` превращён в `_sizes_index`, который отдаёт и схемы, и множество имён из файла размеров. Типы объектов показываются по-русски (`kind_ru` / `OBJECT_KINDS`), в справочнике появились разбивка «каких объектов сколько» и колонка «Тип определён» (колонка / маска / файл размеров / по умолчанию). Проверяется двумя тестами: список масок закреплён, и объект `v_*` с сегментами обязан остаться таблицей. |
| 2026-08-02 | **Сравнение заводов вместо простого свода.** `v_network_overview` собирается из двух независимых сторон — отчёты из файла отчётов, размеры из файла размеров — через `FULL JOIN`: завод бывает в одном файле и отсутствует в другом, и такой РЦ обязан остаться в сравнении, а не исчезнуть. Добавлены объём базы завода, число таблиц, сколько из них под отчётами и сколько без них (в штуках и в МБ), доля объёма под отчётами, число схем, медиана глубины хранения и «МБ на отчёт». Страница «Сети и заводы» перестроена: плитки, стопка «под отчётами / без отчётов» по каждому заводу (две величины одной природы и одной шкалы — честные столбики, а не две оси) и таблица сравнения. Строки-заглушки «(не указана)/(не указан)» из счётчиков сетей и заводов исключены: это не сеть и не завод, а незаполненная колонка. `SCHEMA_VERSION` поднята до 10. |
| 2026-08-02 | **Документация стала двуязычной.** README, RUNBOOK, ARCHITECTURE и это ТЗ существуют по-русски и по-английски рядом: русский файл — оригинал, английский лежит рядом с суффиксом `.en`, переключатель языка — первой строкой каждого. Отдельные файлы, а не два языка внутри одного: документ, продублированный по разделам, нечитаем сразу на обоих языках. `CLAUDE.md` остался только по-русски — это инструкция агента, а не пользовательская документация. Тест проверяет, что у каждого русского документа есть английская пара с той же структурой заголовков: без него две версии расходятся за пару коммитов, и английская тихо превращается во враньё. |
| 2026-08-02 | **Выпущена версия 1.0.0.** Версия живёт в двух местах — `pyproject.toml` и `config.VERSION` — и обязана совпадать: `VERSION` пишется в `etl_run.tool_version`, то есть по любой базе видно, каким кодом она собрана, и разъехавшиеся версии превращают эту запись во враньё. Проверяется `test_version_is_the_same_everywhere`. Заведена история версий `CHANGELOG.md` / `CHANGELOG.en.md` по формату Keep a Changelog: туда идёт то, что заметит пользователь, а подробности и обоснования остаются здесь, в журнале. Не путать с `SCHEMA_VERSION`: та отвечает за структуру базы и поднимается при каждой правке SQL, независимо от версии программы. Состояние помечено тегом `v1.0.0`; дальнейшая работа накапливается в разделе «Не выпущено». |
| 2026-08-02 | **Кнопка очистки базы** на странице загрузки. Стирает все данные и оставляет пустую базу нужной структуры, а не удаляет файл: иначе каждая страница встречала бы пользователя ошибкой «база не найдена» вместо честных нулей, а версия структуры сбросилась бы в ноль и добавила второе непонятное предупреждение. Резервная копия `.bak` удаляется вместе с базой: очистку просят, когда рабочих данных на машине быть не должно, и копия рядом сохранила бы ровно то, что просили стереть — флажок оставляет её тем, кому нужна страховка. Исходные файлы в `data/raw/` не трогаются, и страница прямо говорит, сколько их там осталось: это файлы пользователя, а не содержимое базы. Блок стоит выше всех `st.stop()` страницы и свёрнут, а кнопка не нажимается без флажка подтверждения. Та же операция доступна командой `clear` с подтверждением с клавиатуры и ключами `--yes` / `--keep-backup`. Проверка щелчком нашла настоящий сбой: на полностью пустой базе «Обзор» падал на `source_sha256[:12]` — после очистки слепка нет. Заодно `missing_facts_notice()` научился отличать пустую базу от базы без размеров и предлагает загрузить данные вместо бессмысленного «не загружены размеры таблиц». |
| 2026-08-02 | **Передача образа коллеге описана пошагово**, и по дороге найдена дыра: Dockerfile не копировал `.streamlit/`. В контейнере из-за этого пропадала тёмная тема, `backgroundColor` переставал совпадать с `SURFACE` — вокруг сегментов диаграмм появлялась кайма, ровно та, о которой предупреждает правило палитры, — и включалась обратно отправка статистики использования Streamlit, которую в проекте отключили намеренно. Сборка при этом не падала: забытый каталог не ошибка, он просто тихо меняет поведение у получателя. Теперь `COPY .streamlit/` есть в образе, а тест сверяет список копируемых каталогов с тем, что приложение читает в работе, и отдельно следит, чтобы `data/` не попал в `.dockerignore`. В RUNBOOK добавлен раздел про `docker save` / `docker load`: размер файла, предупреждение о рабочих данных внутри, что коллега может и чего не может без подключённой папки (и что подключённая папка полностью заменяет данные образа), как обновлять. |
| 2026-08-02 | **Поиск принимает несколько значений сразу.** Заказчик приходит со списком таблиц — «покажи вот эти двадцать», — а поле искало ровно одну подстроку. Теперь значения разделяются запятой, точкой с запятой или переводом строки, и строка попадает в результат, если подходит хотя бы под одно: это разбор списка, а не пересечение условий, которое по разным таблицам всегда дало бы пусто. Пробел разделителем сознательно не сделан — имена отчётов состоят из нескольких слов, и «Продажи за месяц» распалось бы на три бесполезных обрывка. Поле пришлось заменить с `st.text_input` на `st.text_area`: в однострочное поле список из Excel не вставить, браузер по стандарту вырезает переводы строк, и проверка показала, что колонка из трёх таблиц слипается в одно слово. Высота минимальная, чтобы не съедать экран там, где под таблицей должен помещаться разбор строки. Ненайденные значения перечисляются подписью — при вставке списка это главное: иначе непонятно, то ли имени нет в базе, то ли опечатка. Заодно исправлена давняя ошибка: `str.contains` разбирал запрос как регулярное выражение, поэтому `[dbo].[Orders]` находил строки с буквами d, b, o, а одинокая `*` роняла страницу с «nothing to repeat» — теперь `regex=False`. То же самое в выгрузке для коллег. |
| 2026-08-02 | **Точное совпадение в поиске и отчёты по всему списку таблиц сразу.** Заказчик вставил список таблиц и получил лишние строки: поиск шёл по части имени, а `itm.log` содержится и в `dbo.catalog`, и в `wh1.errlog`, и в `ssaadmin.vtmuserlogins`. Добавлен флажок «Точное совпадение»: имена сравниваются целиком. Имя без схемы тоже считается совпадением (`TRIP_LOG` находит `sdd.trip_log`) — в выгрузках половина имён приходит со схемой, половина без, и заставлять заказчика разбирать список по частям нельзя. Режим не переключается сам по числу значений: неявное поведение «одно значение — так, несколько — иначе» угадывало бы верно почти всегда, а в остальных случаях выглядело бы поломкой. Вместо этого при нескольких значениях в режиме «часть имени» выводится подсказка с тем же примером, что и в жалобе. Второе: разбор по щелчку отвечает на вопрос про одну таблицу, а приходят со списком — на №2 «По таблицам» появился блок «Все отчёты по найденным таблицам» со связями «таблица + отчёт» по всей выборке и выгрузкой в CSV. В выгрузке для коллег точное совпадение сделано одним переключателем в шапке рядом с выбором РЦ, а не флажком на каждой вкладке. |
| 2026-08-02 | **Выгрузка «таблицы → отчёты» в Excel двумя листами.** CSV годится, чтобы утащить строки в другую программу, но заказчик работает в Excel и ждёт готовую книгу. Лист «Отчёты» — строка на отчёт: сколько его таблиц попало в запрос и какие именно, через `;`; лист «Таблицы и отчёты» — те же пары, что на экране. Помощник `build_workbook()` в `app/_shared.py` собирает книгу из словаря «имя листа → таблица» и годится для любой страницы: переименовывает колонки по общему словарю `LABELS` (файл открывают люди, `table_full_name` в шапке им ничего не говорит), выбрасывает суррогатные ключи, подгоняет ширину колонок по содержимому и закрепляет шапку. Имя листа обрезается до 31 знака и очищается от `: \ / ? * [ ]` — Excel такие имена не принимает и открывает файл с ошибкой. Числа остаются числами, поэтому формулы и сортировка работают сразу. Пишется через `openpyxl`, который и так в зависимостях: он читает исходные `.xlsx`, а теперь ещё и пишет. |
| 2026-08-02 | Уточнён состав книги Excel. Лист «Отчёты» — строк по числу отчётов, по одной на каждый завод; в колонке «Таблицы» перечислены **все** таблицы отчёта, а не только попавшие в запрос. Список таблиц берут, чтобы понять, из чего отчёт состоит целиком, — урезанный до запроса перечень отвечал бы на другой вопрос и молча вводил в заблуждение. Сколько именно искомых таблиц отчёт задевает, осталось отдельной колонкой «Из них из запроса»: это ответ на исходный вопрос, и терять его нельзя. Лист «Таблицы» стал зеркалом первого — строк по числу найденных таблиц, отчёты одной ячейкой через `;`. Пары «таблица + отчёт» в файл больше не идут: они видны на экране, а два листа с одинаковым смыслом в разной нарезке только путают. |
| 2026-08-06 | **Выпущена версия 1.5.0.** В неё вошло всё, что накопилось в разделе «Не выпущено» после 1.0.0: поиск по списку значений, точное совпадение имён, отчёты сразу по всем найденным таблицам с выгрузкой в Excel двумя листами, очистка базы из интерфейса и `.streamlit/config.toml` в Docker-образе. Номер выбран заказчиком: по семантическому версионированию накопленного хватает на 1.1.0 — ломающих изменений нет, структура базы осталась на `SCHEMA_VERSION = 10`, старые базы работают после обычной перезагрузки данных, — а шаг сразу до 1.5.0 сделан как отметка «программа заметно ушла от первой версии» и на совместимость не влияет. `VERSION` поднята в `pyproject.toml` и `config.py` одним коммитом: она пишется в `etl_run.tool_version`, и разъехавшиеся значения превратили бы эту запись во враньё. Раздел «Не выпущено» заведён пустым, ссылки сравнения в подвале обоих CHANGELOG переставлены на `v1.5.0`. Состояние помечено тегом `v1.5.0`. |
| 2026-08-06 | **Уникальные отчёты завода — обратная сторона «Похожих отчётов».** Заказчик спросил, сколько на заводе своего: отчётов, которых нет у соседей по ТС. Прежняя витрина `v_report_overlap` сравнивает отчёты только внутри одного завода, потому что один и тот же отчёт на разных площадках — норма, а не дубль; здесь эта норма и есть предмет вопроса, поэтому сравнение зеркальное: только между **разными** заводами **одной** ТС. Сети сравнивать между собой нельзя — они ведут хозяйство независимо, и совпадение отчёта между сетями ничего не говорит о том, что завод делает сам. Уникальность проверяется по двум независимым признакам: тёзка на другом заводе (сравнение по `LOWER(TRIM(...))` — регистр и краевые пробелы в выгрузке разъезжаются, а отчёт один и тот же) и ближайший по набору настоящих таблиц двойник с индексом Жаккара. Порог «не меньше двух общих таблиц» тот же, что в 5.7: одна общая таблица означает общий справочник вроде календаря. Новая витрина `v_report_plant_twin` отдаёт **факты, а не приговор**: тёзок, ближайшего двойника и величину сходства, а порог уникальности задаёт читатель ползунком на странице — зашитый в SQL порог создал бы две разные правды об уникальности. Строка в витрине одна на отчёт, а не на пару: иначе суммы объёма и запусков выросли бы на пустом месте, и это закреплено тестом. Отдельно назван завод, у которого в ТС нет соседей: `plants_compared = 0`, и «все отчёты уникальны» там означает отсутствие данных для сравнения, а не вывод — без этой оговорки страница врала бы самым уверенным тоном. На демо-данных проверить правила нечем (наименования там уникальны по построению, а сходство не доходит до 0.3), поэтому для тестов собрана отдельная крошечная база, где каждый случай заведён вручную: тёзки в одной ТС, полное совпадение наборов таблиц, одна общая таблица-справочник и полный двойник в чужой ТС. `SCHEMA_VERSION` поднята до 11. |
| 2026-08-06 | **Исправлена посылка предыдущей записи: сети сравнивать между собой не только можно, но и нужно.** Прежнее решение исходило из того, что сети ведут хозяйство независимо и совпадение отчёта между ними ничего не значит, поэтому уникальность считалась только внутри ТС. Заказчик поправил: сети пересекаются по отчётам примерно на 70%, и главный вопрос — чем они **различаются**; внутри одной сети уникальных отчётов по заводам как раз заметно меньше. Область сравнения перестала быть принципом и стала выбором: в витрине это колонка `scope` (`NETWORK` — другие ТС, `PLANT` — другие заводы своей ТС), на странице — переключатель, по умолчанию межсетевой разрез. Витрина `v_report_plant_twin` переименована в `v_report_twin` (строка на «отчёт × область»), рядом появилась `v_network_report_twin`. Вторая нужна из-за единицы счёта: для вопроса «чем различаются сети» отчёт, стоящий на трёх заводах одной ТС, — это **один** отчёт этой сети, и считать его трижды значило бы завысить «своё» втрое; поэтому там заводы схлопнуты, таблицы берутся объединением по заводам, а объём и запуски — суммой. Двойник в других сетях определяется по худшему для уникальности случаю: нашла двойника хотя бы одна площадка — значит отчёт в других сетях есть. Заодно исправлены демо-данные: наименования в них были уникальны по построению, из-за чего страница на демо-базе показывала 100% уникальных и не проверяла ничего. Добавлены «сетевые» отчёты — одно наименование в разных ТС и на нескольких заводах сети, часть копий расходится по одной таблице, чтобы порог сходства был виден в работе. Что осталось прежним: сравниваются только настоящие таблицы, порог «не меньше двух общих таблиц», пара обязана быть между разными РЦ (внутри завода похожие отчёты ищет `v_report_overlap`), тёзки сравниваются по `LOWER(TRIM(...))`, а порог уникальности задаёт читатель, а не витрина. `SCHEMA_VERSION` поднята до 12. |
| 2026-08-06 | **Занятая база валила приложение целиком, а не только текущий запрос.** DuckDB не даёт открыть один файл одновременно на чтение и на запись — у файла ровно один держатель конфигурации. Три места в коде открывали соединение «в лоб», без защиты: кэшированный `connect()` в `_shared.py` (использует каждая аналитическая страница) и два прямых вызова на странице «Загрузка данных». Если в момент такого вызова файл занят — например, в соседней вкладке того же приложения идёт `build()` или `clear()`, которые держат файл на запись секундами, — `duckdb.connect(read_only=True)` падает `duckdb.ConnectionException`, необработанной, и рушит весь скрипт Streamlit-сессии трассировкой на весь экран. Хуже того, это ломало саму страницу «Загрузка данных», которая по описанию в коде обязана быть путём восстановления при любом сбое чтения — получалось, что путь восстановления сам требовал восстановления. Добавлена `try_read_only_connect()`: три попытки с короткой паузой (до ~0.9 с — под быстрые внутренние гонки, не под всю пересборку) и `None` вместо необработанного исключения. `connect()` при `None` показывает `st.error` и `st.stop()` — так же, как уже делает при отсутствующей базе или устаревшей структуре. На странице «Загрузка данных» это осознанно другой сценарий: `st.stop()` недопустим, кнопка «Загрузить» должна остаться нажимаемой, поэтому оба места показывают `st.info`/`st.caption` и продолжают рендерить страницу дальше. Гонка воспроизведена вживую: отдельный процесс держал файл на запись, пока Playwright открывал «Обзор» и «Загрузка данных» — обе страницы показали предупреждение вместо трассировки, а «Шаг 1. Файлы» остался доступен; после освобождения файла обычный `F5` вернул страницы к работе без перезапуска приложения. |
| 2026-08-06 | **Файл статистики без «Каталога» терял строки при совпадающих именах отчётов.** Заказчик показал реальную форму такого файла: колонки ТС, Завод и Наименование отчёта — без единого столбца «Каталог», который в основном файле собирается из трёх уровней вложенности и в `_load_report_usage` никогда не строился заново. Сопоставление отчёта шло по двум уровням: полный путь `(сеть, завод, каталог, имя)` — недостижим без «Каталога» в файле статистики, — и сразу имя без завода `by_name`, где несколько кандидатов означают отказ от сопоставления. Между ними не было уровня, использующего ТС и Завод из файла напрямую, хотя они там есть. На демо-данных, где после добавления «сетевых» отчётов имя намеренно повторяется на нескольких заводах, это давало 18 потерянных строк статистики из 175 — тихо, без предупреждения заметнее, чем `usage_unmatched` в статистике загрузки. Добавлен промежуточный уровень `(сеть, завод, имя)`: он проверяется после полного пути и перед голым именем, использует то, что в файле обычно есть, и не требует «Каталога». Неоднозначность внутри одного завода (тот же отчёт дважды в разных каталогах — редкий, но допустимый случай по ключу `dim_report`) по-прежнему приводит к отказу от сопоставления, а не к угадыванию. После правки на демо-данных потерянных строк — ноль. Закреплено тестом на синтетической паре отчётов с одинаковым именем на двух заводах одной ТС: обращения обязаны попасть каждое на свой завод, а не перепутаться и не потеряться. Изменение только в ETL, `SCHEMA_VERSION` не поднималась — перезагрузка данных нужна лишь тем, кто пользуется отдельным файлом статистики без «Каталога». |
| 2026-08-06 | **Статистика использования — только отдельный файл, и добавлен текст SQL-запроса.** Заказчик поправил решение из предыдущей записи: колонки «Кол-во обращений» и «Ср. дл. (сек)» в основном файле отчётов физически остаются (старый формат выгрузки, менять его никто не будет), но читать их оттуда больше нельзя — единственный источник статистики теперь файл `report_usage`. Причина: основной файл присылают реже, чем меняется статистика, и данные в нём устаревают, а значит рискуют молча выдаваться за актуальные. Убраны чтение `exec_count`/`avg_duration_sec`/`avg_duration_ms` из `reports.columns` (`config/mapping.yml` и `config/mapping.sample.yml`) и вся вставка в `fact_report_usage` внутри `_load_reports` — раньше файл размеров недостающая колонка тихо подменялась пустой, теперь колонки нет вовсе, и подменять нечего. Заодно упрощён комментарий `INSERT OR REPLACE` в `_load_report_usage`: раньше он объяснял перекрытие значений основного файла, которого больше не существует, а REPLACE остаётся ради защиты от конфликта первичного ключа при повторном сопоставлении. Демо-файл `sample_reports.xlsx` намеренно сохранил эти колонки заполненными: это и есть проверка того, что они действительно игнорируются, а не просто отсутствуют.

Вторая часть — новый файл `report_sql`: наименование отчёта и текст SQL-запроса, показывается на отдельной вкладке карточки отчёта. У файла нет ТС и Завода — запрос описывает определение отчёта, а не конкретную площадку, поэтому сопоставление идёт только по имени, и найденный текст **применяется сразу ко всем `report_id` с этим именем**, а не только к одному «наиболее вероятному». Это осознанно другое правило, чем у статистики использования: там неоднозначное имя — повод отказаться от сопоставления (данные ушли бы не туда), а здесь применить один и тот же запрос к отчёту-тёзке на другом заводе — не ошибка, а точный смысл данных. `dim_report` получила колонку `sql_text` (`SCHEMA_VERSION` поднята до 13), `_load_report_sql` пишет её через `UPDATE ... WHERE report_id = ?` после того, как `_load_reports` уже вставила строки — отдельным проходом, а не витриной: `sql_text` читается в карточке отчёта прямым запросом к `dim_report`, без промежуточного view, как и `description`/`owner`. Ненайденные по имени наименования копятся в `stats.sql_unmatched`, как и у статистики. В самодостаточный HTML-экспорт вкладка с запросом пока не перенесена — там карточка отчёта на JS-шаблоне, а не на Python, и это отдельная по объёму работа. |
| 2026-08-06 | **Файл SQL-запросов повторяет структуру файла отчётов, и для запросов появилась своя страница.** Заказчик показал фактический формат: `№`, `ТС`, `Завод`, `Каталог 1/2/3-го уровня`, `Наименование отчета`, `Запрос к базе данных` — то есть в файле есть организационный разрез, которого я в предыдущей записи не предполагал. Это меняет правило сопоставления к лучшему: вместо «раздать текст всем тёзкам» работают три уровня, как у статистики использования, — `(сеть, завод, каталог, имя)` → `(сеть, завод, имя)` → одно имя. Путь собирается из трёх уровней тем же `join_folders`, что и в файле отчётов. Прежнее поведение сохранено как ветка для файлов **без** ТС и Завода: там имя по-прежнему раздаётся всем тёзкам, потому что запрос описывает определение отчёта, а не площадку. Если же ТС с Заводом в файле есть, но строка ими не сопоставилась, голое имя принимается только когда оно единственное в базе: иначе запрос ушёл бы на чужой завод, у которого свой. Оба правила закреплены отдельными тестами. Обновлены демо-данные: `sample_report_sql.xlsx` теперь несёт ТС, Завод и каталог, а запрос упоминает первую таблицу отчёта — на такой строке видно, что поиск по тексту запроса действительно находит отчёты по имени таблицы.

Вторая часть — страница **«Инструменты → Запросы к БД»** (`report_sql.py`). Отдельная страница, а не только вкладка карточки: карточка отвечает «всё про этот отчёт», а здесь обратный порядок — искать **по тексту запроса**, и поиск включает `sql_text`. Так отвечается вопрос «какие отчёты трогают эту таблицу» для таблиц, не попавших в колонку «Таблицы источники данных»: обращения через `JOIN` в подзапросе по списку источников не видны вовсе. Под запросом показаны заявленные источники отчёта с отметкой «есть в запросе» — расхождение между списком из файла и текстом и есть то, что страница помогает заметить; сравнение идёт и по имени без схемы, потому что в запросах таблицу пишут двояко, а «нет» бывает законным (объект приходит через view или процедуру). Медиана строк в запросе, а не среднее: один запрос на тысячу строк перекосил бы среднее. `SCHEMA_VERSION` не поднималась — колонка `sql_text` появилась в предыдущей правке, здесь менялись только маппинг, ETL и интерфейс. |
